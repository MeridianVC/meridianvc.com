// used in other components for height calcs
// cannot be exported directly from Navbar due to it being a client-side component

const navHeight = 'clamp(80px, calc(7vh + 2px), 110px)'; 

export { navHeight }
